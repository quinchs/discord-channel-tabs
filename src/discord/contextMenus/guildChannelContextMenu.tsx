/** @jsx jsx */
import {css, jsx} from '@emotion/react'
import Popout from "../popout";

type Props = {
    
};
export const GuildChannelContextMenu = (props: Props) => {
    return (
        <Popout
            align={"center"}
            position={"bottom"}
        >
            
        </Popout>
    );
};